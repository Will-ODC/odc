{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "dependsOn": ["^build"], "outputs": ["dist/**"] },
    "test": {
      "dependsOn": ["build"],
      "env": ["CI", "DATABASE_URL"]
    },
    "lint": {},
    "typecheck": { "dependsOn": ["^build"] }
  }
}
