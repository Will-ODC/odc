# verifier

**Status: not started. Blocked on contracts/ freeze (Phase 0). Phase: 1.**

Standalone Go CLI verifier. Built from contracts/ alone.

See `docs/implementation-plan.md` §"verifier" for the full spec, and this
directory's `CLAUDE.md` for agent rules. On first commit, consult the
`odc-service-boundaries` and `odc-pipeline` skills: every service ships
README.md, API.md, src/, tests/.
