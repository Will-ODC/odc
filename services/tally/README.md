# tally

**Status: not started. Blocked on contracts/ freeze (Phase 0). Phase: 2.**

All derived views. Holds no truth; rebuildable from the export at any time.

See `docs/implementation-plan.md` §"tally" for the full spec, and this
directory's `CLAUDE.md` for agent rules. On first commit, consult the
`odc-service-boundaries` and `odc-pipeline` skills: every service ships
README.md, API.md, src/, tests/.
