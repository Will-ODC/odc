# mcp

**Status: not started. Blocked on contracts/ freeze (Phase 0). Phase: 3.**

Thin protocol wrapper over the substrate. Contains no logic of its own.

See `docs/implementation-plan.md` §"mcp" for the full spec, and this
directory's `CLAUDE.md` for agent rules. On first commit, consult the
`odc-service-boundaries` and `odc-pipeline` skills: every service ships
README.md, API.md, src/, tests/.
