{
  "name": "@odc/pulse",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pulse — charter-exempt epic: email-claim voting with a one-screen story UI.",
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "dev": "pnpm build && node dist/src/dev-server.js",
    "test": "node --test dist/test/*.test.js",
    "lint": "eslint src test",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/pg": "^8.11.0"
  },
  "dependencies": {
    "@fastify/cookie": "^11.1.2",
    "@fastify/rate-limit": "^11.2.0",
    "fastify": "^5.11.3",
    "pg": "^8.13.0"
  }
}
